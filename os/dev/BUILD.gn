module_name = get_path_info(rebase_path("."), "name")

group(module_name) {
    deps = []
    deps += [
        "uart",
    ]
}

config("kernel") {
    configs = []
    configs += [
        "uart:kernel",
    ]
}